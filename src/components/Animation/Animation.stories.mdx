import { Meta, Story } from '@storybook/addon-docs';
import { Text } from "../Text"
import { Animation } from '.';

<Meta title="Components/Animation" component={Animation} />

export const Template = (args) => <Animation {...args} />;

<Story
  name="alert"
  args={{ 
    variant: "alert", 
    children: <Text>Alert</Text>,
    duration: 10,
  }}
>
  {Template.bind({})}
</Story>

<Story
  name="modal-open"
  args={{ 
    variant: "modal-open", 
    children: <Text>Modal Open</Text>,
    duration: 2,
  }}
>
  {Template.bind({})}
</Story>

<Story
  name="modal-close"
  args={{ 
    variant: "modal-close", 
    children: <Text>Modal Close</Text>,
    duration: 2,
  }}
>
  {Template.bind({})}
</Story>